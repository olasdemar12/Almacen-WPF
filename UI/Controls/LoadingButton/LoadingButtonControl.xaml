<UserControl x:Class="StockMasterControls.LoadingButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockMasterControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Name="Root">
    <Grid Background="Transparent">

        <Button Width="{Binding Width,ElementName=Root}"
                Height="{Binding Height,ElementName=Root}"
                
                Background="{Binding ButtonColor,ElementName=Root}"
                Foreground="{Binding Foreground,ElementName=Root}"
                BorderBrush="{Binding ButtonColor,ElementName=Root}"
                
                BorderThickness="{Binding BorderThickness,ElementName=Root}"
                Padding="{Binding Padding,ElementName=Root}"
                FontSize="{Binding FontSize,ElementName=Root}"
                FontWeight="{Binding FontWeight, ElementName=Root}"
                
                materialDesign:ButtonAssist.CornerRadius="5"
                
                Command="{Binding Command, ElementName=Root}"
                CommandParameter="{Binding CommandParameter, ElementName=Root}"
                x:Name="btn"
                Click="Button_Click">
            <Grid>
                <ContentPresenter Content="{Binding ButtonContent, ElementName=Root}">
                    <ContentPresenter.Style>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading,ElementName=Root}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentPresenter.Style>
                </ContentPresenter>
                
                <!--Barra de Progreso-->
                <ProgressBar IsIndeterminate="True" Width="24" Height="24" Foreground="{Binding Foreground, ElementName=Root}">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading,ElementName=Root}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
            </Grid>
        </Button>
        
    </Grid>
</UserControl>
