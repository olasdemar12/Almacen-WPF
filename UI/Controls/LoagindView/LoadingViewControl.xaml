<UserControl x:Class="StockMasterControls.LoadingViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockMasterControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root">
    
    <UserControl.Style>

        <Style TargetType="UserControl">
            
            <Setter Property="Visibility" Value="Visible"/>

            <Style.Triggers>
                
                <!--Propiedades que toma el control cuando esta "Cargando"-->
                <DataTrigger Binding="{Binding IsLoading,ElementName=Root}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="IsHitTestVisible" Value="True"/>
                </DataTrigger>
                
                <!--Animacion de salida (Fade) del control-->
                <DataTrigger Binding="{Binding IsLoading, ElementName=Root}" Value="False">
                    <Setter Property="IsHitTestVisible" Value="False"/>
                    
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>

                                <!--Animacion de Opacidad para el efecto Fade-->
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <QuinticEase EasingMode="EaseInOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>

                                <!--Se Aplica el estado "Visibility" en Collapse luego de animar la Opacidad-->
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.3" Value="{x:Static Visibility.Collapsed}"/>
                                </ObjectAnimationUsingKeyFrames>

                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                </DataTrigger>
                
                
            </Style.Triggers>

        </Style>
    </UserControl.Style>
    
    <Grid HorizontalAlignment="{Binding HorizontalAlignment, ElementName=Root}" 
          VerticalAlignment="{Binding VerticalAlignment, ElementName=Root}"
          Background="White">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <ProgressBar IsIndeterminate="True" Width="100" Height="100"
                         Foreground="{Binding ProgressColor, ElementName=Root}"
                         Style="{StaticResource MaterialDesignCircularProgressBar}"/>

            <TextBlock Text="{Binding TextLoading, ElementName=Root}" Margin="0,10,0,0" HorizontalAlignment="Center"
                       FontSize="22" FontFamily="Segoe UI" FontWeight="Normal"
                       Foreground="{Binding TextColor, ElementName=Root}"/>
            
        </StackPanel>

    </Grid>
    
</UserControl>
